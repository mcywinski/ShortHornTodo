<UserControl x:Class="ShortHorn.Desktop.UserControls.TodoItemRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid Height="30">
        <CheckBox x:Name="checkboxTodoItemComplete" IsChecked="{Binding IsFinished}" HorizontalAlignment="Left" Width="15" Margin="0,6,0,4" VerticalAlignment="Center" Checked="checkboxTodoItemComplete_Checked" />
        <Label Margin="20,0,10,4" VerticalAlignment="Center" Content="{Binding Name}" />
    </Grid>
</UserControl>
